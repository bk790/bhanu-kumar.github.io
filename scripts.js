// For now, we're not using much JavaScript in this portfolio.
// You can later add features like a contact form, interactive elements, or animations.

// Example: Adding simple interactivity for future enhancements
document.addEventListener('DOMContentLoaded', () => {
    // Example: Changing the background color on click (could be expanded for interactivity)
    document.querySelector('h1').addEventListener('click', () => {
        document.body.style.backgroundColor = '#e0f7fa';
    });
});
